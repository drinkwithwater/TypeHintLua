#!/usr/bin/lua

local filename = arg[1]
if not filename then
  print("usage : the xxx.thlua")
end

local thlua = require "thlua"
local runtime = thlua.createRuntimeByFile(filename)
assert(runtime:main())

-- print("============later call============================")
--runtime:checkDefineLuaFunction()
--runtime:checkTypeFinish()
--runtime:checkLateLuaFunction()

