
const function test()
	const t = {.open }

	setmetatable(t, {
		__index=t,
		__newindex=t,
	})

	const s = t[1] --E
end

const function test()
	const function:class(let.Dosth) dosth()
		return setmetatable({.class
			fds=31
		}, {
			__index={} @ Dosth
		})
	end

	const t = dosth()

	ttprint(t.rewr) --E
end

