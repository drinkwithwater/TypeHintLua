
const Exception = require "thlua.Exception"
(@let.clazz = import("thlua.type.basic.TypeClass").clazz)

const DoBuilder = {}
DoBuilder.__index=DoBuilder

function:class(let.DoBuilder) DoBuilder.new(vContext:clazz.BaseStack, vNode:clazz.IAstNode)
	return setmetatable({.class
		pass=false@Boolean,
	}, DoBuilder)
end

function DoBuilder:build(vHintInfo:clazz.TLongHintInfo(Nil)):Ret()
	if vHintInfo.attrSet.pass then
		self.pass = true
	end
end

return DoBuilder
