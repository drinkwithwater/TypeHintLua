

const class = require "thlua.class"

(@do
	let.clazz = import($class).clazz
end)

const NameLocation = class@<clazz.NameLocation, false>()

function NameLocation.__tostring(self:clazz.NameLocation)
    return "NameLocation tostring TODO"
end

function.open NameLocation:ctor(vSpace:clazz.Namespace, vKey:clazz.UAllType)
    self._space = vSpace
    self._key = vKey
end

return NameLocation